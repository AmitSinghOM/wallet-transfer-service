# Copy to .env (never commit .env). All config is environment-driven (12-factor).
DATABASE_URL=postgresql://wallet:wallet_local_only@localhost:5432/wallet
JWT_SECRET=change-me-to-a-long-random-string
APP_ENV=development
TOKEN_TTL_SECONDS=86400
WELCOME_GRANT_PAISE=100000
DB_POOL_MIN=2
DB_POOL_MAX=10
PORT=8000
